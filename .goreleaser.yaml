# Melliza - GoReleaser Configuration
# https://goreleaser.com
#
# Build targets:
#   - darwin/amd64, darwin/arm64
#   - linux/amd64, linux/arm64
#   - windows/amd64
#
# Local snapshot (builds all platforms):
#   goreleaser release --snapshot --clean
#
# Single target (current platform only):
#   goreleaser build --snapshot --clean --single-target

version: 2

before:
  hooks:
    - go mod tidy

builds:
  - main: ./cmd/melliza
    binary: melliza
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
      - -X main.Version={{.Version}}

archives:
  - id: melliza-archive
    formats:
      - tar.gz
    # Use zip for Windows
    format_overrides:
      - goos: windows
        formats:
          - zip
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}
    # Files to include in the archive
    files:
      - LICENSE*
      - README*

checksum:
  name_template: 'checksums.txt'

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'
      - Merge pull request
      - Merge branch

# Announce release notes style
release:
  draft: false
  prerelease: auto
  name_template: "Melliza v{{.Version}}"

# Homebrew tap configuration
brews:
  - name: melliza
    # Repository to push the formula to
    repository:
      owner: lvcoi
      name: homebrew-melliza
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    directory: Formula
    homepage: "https://lvcoi.github.io/melliza"
    description: "Autonomous agent loop for working through PRDs with Gemini CLI"
    license: "MIT"
    # Custom install script
    install: |
      bin.install "melliza"
    # Test block
    test: |
      assert_match "melliza", shell_output("#{bin}/melliza --version")
    # Skip upload on snapshot builds
    skip_upload: auto
